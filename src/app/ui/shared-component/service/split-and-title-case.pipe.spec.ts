import { SplitAndTitleCasePipe } from './split-and-title-case.pipe';

describe('SplitAndTitleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitAndTitleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
